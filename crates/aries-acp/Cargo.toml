[package]
name = "aries-acp"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
serde_json.workspace = true
tokio = { workspace = true, features = ["rt", "io-std", "sync"] }

agent-client-protocol = "0.10"
async-trait = "0.1"
tokio-util = { version = "0.7", default-features = false, features = ["compat"] }
tracing = "0.1"
