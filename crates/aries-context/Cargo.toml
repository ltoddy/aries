[package]
name = "aries-context"
version = "0.1.0"
edition = "2024"

[dependencies]
aries-filesystem = { path = "../aries-filesystem" }
rig.workspace = true
tokio = { workspace = true, features = ["sync"] }
tracing.workspace = true
