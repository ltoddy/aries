[package]
name = "aries-persistence"
version = "0.1.0"
edition = "2024"

[dependencies]
jiff.workspace = true
toasty = { workspace = true, features = ["jiff", "sqlite"] }
toasty-driver-sqlite.workspace = true
