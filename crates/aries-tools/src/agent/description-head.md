启动一个新的子智能体（subagent）来自主完成复杂的多步任务。

Agent 工具会启动专门的子智能体（子进程）自主处理复杂任务，每种类型的智能体都拥有各自的能力和可用工具。

内置的 `mode` 取值：
- `default` / `build`：标准的 Aries 智能体，拥有完整工具集
- `explore`：擅长在大型代码库中快速定位与调研（只读）
- `plan`：以阅读和规划为主，不进行编辑
- `general`：通用回退；当 `mode` 未匹配到任何类型时会被映射为此类型
