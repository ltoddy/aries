从 URL 抓取页面内容并以文本返回。

- `http://` 会被自动升级为 `https://` 后再请求。
- 抓取后以 UTF-8 文本读取整个响应体；如果超过 10000 字符会被截断，并附加 `... (content truncated due to length)` 标记。
- `format` 参数目前仅在 schema 中保留，**当前实现不会做格式转换**，始终返回原始响应文本。
- 工具是只读的，不会修改任何本地文件。
- 当存在更适合具体任务的抓取/搜索工具时，请优先使用那些工具。
