从本地文件系统读取文本文件内容。如果路径不存在或目标不是可按文本读取的文件，将返回错误。

用法：
- `file_path` 参数是要读取的文件路径。
- `offset` 参数是开始读取的行号（从 1 开始）。
- 如果提供了 `offset`，工具会跳过前面的行并返回剩余内容。
- 当前实现返回原始文本内容，不会为每一行添加行号前缀。
- 当前实现不会自动截断到固定行数，也不会专门处理目录、图片或 PDF。
- 如果您不确定正确的文件路径，请先使用 `glob` 工具查找文件名。
- 如果您知道有多个文件需要读取，可以并行调用此工具。
