从本地文件系统读取文本文件内容。

- `file_path` 必需，指向要读取的文件。
- `offset` 可选，从 1 开始的行号；提供后会跳过 `offset - 1` 行，返回剩余文本。
- 返回原始文本，行之间以 `\n` 连接。
- 不会处理目录、二进制、图片或 PDF；这些情况会以 IO 错误返回。
- 如果不确定文件路径，可先用 `Glob` 定位。
- 多个独立文件可以并行调用此工具。
