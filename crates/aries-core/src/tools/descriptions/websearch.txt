**注意：当前是占位实现，并未接入真实搜索 API。** 调用本工具不会执行真正的网络搜索，只会返回一段提示，说明需要接入 Tavily / Exa 等搜索服务才能启用。

- `query` 必需，为搜索关键字。
- `num` 可选，未来用于限制返回结果数量；当前未生效。
- 工具是只读的，不会修改任何文件。

在该工具完成实现前，需要外部信息时优先使用 `WebFetch`（已知 URL）或 `CodeSearch`（第三方库/SDK 上下文）。
