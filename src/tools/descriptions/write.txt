将文件写入本地文件系统。

用法：
- 如果提供的路径处已有现有文件，此工具将覆盖它。
- 如果这是一个现有文件，您必须先使用 Read 工具读取文件的内容。如果您没有先读取文件，此工具将失败。
- 始终优先编辑代码库中的现有文件。除非明确要求，否则切勿写入新文件。
- 绝不主动创建文档文件 (*.md) 或 README 文件。只有在用户明确要求时才创建文档文件。
- 只有在用户明确要求时才使用表情符号。除非被要求，否则避免将表情符号写入文件。